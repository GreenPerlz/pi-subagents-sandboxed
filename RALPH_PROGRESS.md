- 2026-06-03 issue #2: Added sandbox config schema/frontmatter/settings parsing and deterministic resolver; focused sandbox/schema tests pass. Full unit suite was run and has unrelated pre-existing failures noted in final report.
- 2026-06-03 issue #3: Added internal sandbox provider abstraction plus Bubblewrap host-toolchain invocation builder with mount/env/network/fallback unit coverage; focused sandbox tests pass. Full unit suite still has unrelated pre-existing failures (agent frontmatter fork default and Node --experimental-transform-types child-registration tests).
- 2026-06-03 issue #4: Wired explicit per-run Bubblewrap sandboxing into fresh foreground single subagent execution, preserving unsandboxed spawn behavior; added fake-bwrap integration coverage and clear setup-error coverage. Focused single-execution and sandbox tests pass under --experimental-strip-types; configured --experimental-transform-types command remains unsupported on this Node, and full unit suite still has known unrelated failures.
- 2026-06-03 issue #4 reviewer fixes: Stopped serializing inherited foreground env into Bubblewrap argv while preserving child env inheritance, and added read-only mounts for absolute generated --extension parents. Added integration assertions for fake secret argv absence/child env presence and extension parent ro-bind coverage; required focused validation passes.
- 2026-06-03 issue #5: Added reusable subagent sandbox mount policy for fresh/forked child sessions, prompt temp files, outputs/progress/status/artifacts, and extension parents; wired foreground and async single Bubblewrap child execution through it. Focused sandbox, single, and async validation pass; full unit suite still has the known unrelated agent-frontmatter and --experimental-transform-types subprocess failures.

- 2026-06-03 issue #5 worker fix: plumbed resolved sandbox config into foreground/async chain and parallel child execution, added progress path mount wiring for sandboxed children, and covered foreground chain/parallel plus async chain/parallel sandbox cases with fake-bwrap integration tests. Validation: unit sandbox tests, focused chain/parallel/async integration tests, and git diff --check passed.
- 2026-06-03 issue #6: Added sandbox cwd/worktree write inference from edit/write/bashWrite, made read-only sandboxed children mount cwd/worktree read-only, and added fail-closed guards for sandboxed parallel writers without worktree isolation while preserving read-only parallel runs. Focused sandbox, single, parallel, chain, and async validation passed under --experimental-strip-types.
- 2026-06-03 issue #6 reviewer fixes: made omitted tools fail closed as writable, resolved sandbox per agent/task for foreground and async parallel/chain execution, rejected writable dynamic fanout under sandboxing, and upgraded duplicate ro/rw sandbox mounts so explicit writable progress/output parents are not suppressed. Validation: unit sandbox tests, focused single/parallel/chain/async integration tests, and git diff --check passed.
- 2026-06-03 issue #6 dynamic fanout reviewer fix: allowed runner-materialized dynamic parallel templates to carry resolved sandbox config and added async sandboxed read-only dynamic fanout regression coverage while preserving writable dynamic fanout fail-closed rejection. Validation: focused async integration test, unit sandbox tests, and git diff --check passed.
- 2026-06-03 issue #7: Documented pi-subagents-sandboxed replacement/sandbox configuration and surfaced sandbox diagnostics (provider/profile/network/auth/fallback/fallbackOccurred) in foreground results plus async status/result payloads; missing Bubblewrap errors now point to README/PRD docs. Focused sandbox, single, and async validation passed under --experimental-strip-types.
- 2026-06-04 issue #8: Added closed Pi runtime for sandboxed children (--no-extensions, --no-prompt-templates, --no-themes with explicit runtime extensions only), preferred absolute node + resolved Pi package bin for sandboxed spawn (skipping argv1 shortcuts), and mount-policy coverage for spawn runtime paths, extension parents, and pi-json auth. Committed separately from issue #9.
- 2026-06-04 issue #9: Added Bubblewrap read-only mount for /run/systemd/resolve when /etc/resolv.conf resolves into it and network is "host", fixing DNS fetch failures on systemd-resolved hosts inside sandboxes. Mount is skipped for network "none" or when the directory is absent. Includes positive/negative unit coverage. Committed cleanly on top of issue #8.
- 2026-06-04 issue #10: Added sandbox access diagnostics for missing executables and EACCES/EPERM/EROFS filesystem failures, redacted mount summaries in result/status/log payloads, explicit extra read-only/writable mount configuration, and docs for an agent-assisted safe-access smoke-test workflow. Focused sandbox/schema unit tests and git diff --check passed; full unit suite still has known unrelated default-context and Node --experimental-transform-types failures.
- 2026-06-04 issue #10 reviewer fixes: wired `extraReadOnlyMounts` and `extraWritableMounts` into foreground `buildSubagentSandboxMounts` call (was already passed in async/background), and added `diagnoseSandboxFailure` to foreground failed sandbox runs so `execvp`/`EACCES`/`EPERM`/`EROFS` errors populate `result.sandbox.diagnostics` consistently with background execution. Added fake-bwrap argv integration test for extra mounts; focused sandbox unit tests, single-execution integration test, and git diff --check passed. Full suite still has 3 pre-existing unrelated sandbox-environment failures on this Node runtime.
- 2026-06-04 issue #10 reviewer fix 2: merged `wrapped.mounts` into `effectiveSandboxMounts` in foreground execution before calling `diagnoseSandboxFailure`, mirroring the background merge that Bubblewrap provider already performs. This ensures implicit provider mounts such as `/usr`, `/bin`, `/etc`, and the node root are visible to foreground diagnostics, so `execvp`/`EROFS` failures on paths covered by provider mounts are correctly classified instead of suggesting unnecessary extra mounts. Added `installFailingFakeBwrap` integration test helper and regression test verifying a foreground sandbox failure diagnostic recognizes a path covered by an implicit provider mount. Focused new integration test, unit sandbox-diagnostics tests, and git diff --check passed.
- 2026-06-04 issue #11: Added sandbox packageDiscovery support with closed/project-local/ambient config plumbing, parent-side project-local Pi package extension resolution, explicit child --extension flags, read-only package-root mounts, and docs for discovery modes/security tradeoffs. Focused project-local sandbox integration plus sandbox config/schema/pi-args/mount-policy tests pass under --experimental-strip-types; full unit still has known unrelated agent-frontmatter and --experimental-transform-types child-registration failures.
- 2026-06-04 issue #11 reviewer FIX_NOW: Constrained project-local settings package roots to the project/repo-local boundary (rejects ~, absolute, and ../-escaped roots) and constrained package manifest extension paths to stay within the package root (rejects absolute and escaped entries). Added focused TDD unit coverage in test/unit/pi-packages.test.ts; all 4 new tests plus existing sandbox-mount-policy tests pass. Sandbox-config test fails only due to missing typebox dependency (environmental). Committed cleanly on branch ralph/issue-11-project-local-packages.
- 2026-06-04 issue #11 reviewer FIX_NOW (symlink escapes): Canonicalized project-local package roots and extension paths with `fs.realpathSync` so symlinked package roots that escape the project boundary or contain symlinked extension escapes are rejected, while legitimate non-symlink and in-boundary symlink packages are preserved. Added 3 focused TDD unit tests; all 7 pi-packages tests plus sandbox-mount-policy and bubblewrap unit tests pass. git diff --check clean. Committed on branch ralph/issue-11-project-local-packages.
- 2026-06-04 issue #13: Fixed stale `needs_attention` state in async subagents by clearing per-step `activityState` back to `undefined` in `updateRunnerActivityState` when `deriveActivityState` no longer returns `needs_attention` (i.e., resumed activity). Added two integration tests: one for single-step recovery after mutating-tool-failure attention, and one for parallel async top-level aggregation clearing when a stale child resumes. Focused async execution, async status, async job tracker, and control unit tests pass; full unit suite still has known unrelated agent-frontmatter and intercom-bridge failures. git diff --check clean. Committed on branch ralph/issue-13-needs-attention-reset.
- 2026-06-04 issue #13 reviewer fixes: Fixed live top-level `needs_attention` remaining stale after step-level clearing by basing the reset-loop comparison on `statusPayload.activityState` instead of the potentially unsynced `currentActivityState`. Strengthened existing single and parallel recovery tests to assert recovery while `state === "running"` (not after completion). Added deterministic idle/no-activity threshold recovery test using `needsAttentionAfterMs: 1500`. TDD red-green confirmed: strengthened tests failed before fix, passed after. Focused async execution, async status, async job tracker, and control unit tests pass. git diff --check clean. Committed on branch ralph/issue-13-needs-attention-reset.
